summary: "Agregar categoria"
description: "Insertar nueva categoria"
consumes:
- "application/json"
produces:
- "application/json"
parameters:
- in: "body"
  name: "body"
  description: "Accepts a input dictionary of orders"
  required: "true"
  schema:
  properties:
     order:
       type: "object"
  example: {"descripcion":"nombre","precio":60.0}
responses:
  200:
    schema:
      example: {"idextra": 588}
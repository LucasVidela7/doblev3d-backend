summary: "Agregar categoria"
description: "Insertar nueva categoria"
consumes:
  - "application/json"
produces:
  - "application/json"
parameters:
  - in: "body"
    name: "body"
    description: "Accepts a input dictionary of orders"
    required: "true"
    schema:
    properties:
      order:
        type: "object"
    example: {
      "descripcion": "BOMBILLA ALUMINIO CON LIMPIADOR",
      "precio": 51.0,
      "categorias": [
          576,
          577
      ]
    }
responses:
  200:
    schema:
      example: { "idextra": 588 }
summary: "Borrar categoria"
description: "Borrar categoria y asignar productos a otra ya existente"
consumes:
- "application/json"
produces:
- "application/json"
parameters:
- in: "body"
  name: "body"
  description: "Accepts a input dictionary of orders"
  required: "true"
  schema:
  properties:
     order:
       type: "object"
  example: {"fromIdCategoria":1, "toIdCategoria":2}
responses:
  200:
    schema:
      example: {"mensaje": "categoria borrada"}
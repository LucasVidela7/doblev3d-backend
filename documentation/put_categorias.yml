summary: "Editar producto"
description: "Editar producto producto"
consumes:
  - "application/json"
produces:
  - "application/json"
parameters:
  - in: "body"
    name: "body"
    description: "Accepts a input dictionary of orders"
    required: "true"
    schema:
    properties:
      order:
        type: "object"
    example: {
      "categoria": "nombre"
    }
responses:
  200:
    schema:
      example: {
        "idcategoria": 588
      }